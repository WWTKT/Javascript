/**
 * Created by tony on 2016. 8. 10..
 */
var foo = new Array(3);
console.log(foo);           // Array[3] length만 존재
console.log(foo.length);    // 출력값 3

var bar = new Array(1,2,3);
console.log(bar);           // [1, 2, 3] 원소를 가진 Array[3]
console.log(bar.length);    // 출력값 3
